#ifndef embattle_h
#define embattle_h

#include <string>
#include <vector>

std::vector<int> embattle(
    int n,
    std::vector<int> x,
    std::vector<int> y,
    std::vector<int> e,
    std::vector<std::string> op,
    std::vector<int> arg1,
    std::vector<int> arg2);

#endif
